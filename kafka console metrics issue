oc patch podmonitor kafka-resources-metrics -n dev-kafka --type='json' \
  -p='[{"op": "replace", "path": "/spec/namespaceSelector/matchNames/0", "value": "dev-kafka"}]'

# Verify
oc get podmonitor kafka-resources-metrics -n dev-kafka -o jsonpath='{.spec.namespaceSelector}'



# Add label so user-workload Prometheus scrapes this namespace
oc label namespace dev-kafka openshift.io/user-monitoring=true --overwrite

# Verify
oc get namespace dev-kafka --show-labels






Perfect. Only `dev-kafka` has the wrong PodMonitor. Others don't have it yet. Here is your exact step-by-step plan:

---

## Start Here — Step by Step

---

### Step 1 — Fix PodMonitor namespace (bastion, safe, only touches dev-kafka)

```bash
oc patch podmonitor kafka-resources-metrics -n dev-kafka --type='json' \
  -p='[{"op": "replace", "path": "/spec/namespaceSelector/matchNames/0", "value": "dev-kafka"}]'
```

Verify:
```bash
oc get podmonitor kafka-resources-metrics -n dev-kafka \
  -o jsonpath='{.spec.namespaceSelector}'
```

Expected: `{"matchNames":["dev-kafka"]}`

---

### Step 2 — Label dev-kafka namespace (bastion, safe)

```bash
oc label namespace dev-kafka openshift.io/user-monitoring=true --overwrite
```

Verify:
```bash
oc get namespace dev-kafka --show-labels
```

---

### Step 3 — Read the Console Secret (bastion, read-only)

```bash
oc get secret dev-console-console-secret -n dev-kafka \
  -o jsonpath='{.data.console-config\.yaml}' | base64 -d
```

Share this output here — we need to see the current console config before editing it.

---

### Step 4 — Wait 2-3 minutes after Steps 1 & 2, then verify Prometheus is scraping

```bash
oc exec -n openshift-user-workload-monitoring prometheus-user-workload-0 -- \
  curl -s 'http://localhost:9090/api/v1/query?query=kafka_server_BrokerTopicMetrics_Count' \
  | python3 -m json.tool | head -20
```

---

**Start with Step 1 now and share each output before moving to next step.**
