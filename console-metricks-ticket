Hi Angelo,

Please find the attached kafka.yaml.

Additionally sharing some context for the issue:

Kafka cluster is running fine and brokers are healthy
Metrics are exposed on port 9404
Prometheus is scraping metrics successfully
PodMonitor is configured correctly
Console ServiceAccount has required RBAC (cluster-monitoring-view)

However:

Kafka Console UI loads cluster details
But metrics are not visible in the UI

Requesting your help to identify if anything is missing in Kafka configuration or integration with Console/Prometheus.

Thanks.
